#include <stdio.h>
#include <math.h>

int main(void) {
    int n_case, x, y;
    int res;

    scanf("%d", &n_case);
    for (int i = 0; i < n_case; i ++) {
        scanf("%d %d", &x, &y);
        for (int ii = 0; ii < n_case; ii ++) {
            
        }
    }
}